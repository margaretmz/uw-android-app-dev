<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textViewAppDesc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_desc"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/buttonCreateBasic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewAppDesc"
        android:text="Basic Notification"/>

    <Button
        android:id="@+id/buttonUpdateBasic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonCreateBasic"
        android:text="Update Basic Notification"/>

    <Button
        android:id="@+id/buttonCancelBasic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonUpdateBasic"
        android:text="Cancel Basic Notification"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/buttonBackStack"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonCancelBasic"
        android:text="Notification with Backstack"/>

</RelativeLayout>
